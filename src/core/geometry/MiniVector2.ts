import { Vector2 } from "three";

class MiniVector2 {
    x: number;
    y: number;
    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }
}

export default MiniVector2;